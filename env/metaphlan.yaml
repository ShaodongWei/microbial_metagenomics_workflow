# using conda to install 4.* will fail 
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pip
  - bowtie2
  - numpy
  - pandas
  - biopython
  - pysam  # <-- from conda, pre-built
  - pip:
      - metaphlan==4.1.1
